//using Kingdom.OrTools.Sat.CodeGeneration.Attributes;

///* Which is informed by the `*CodeGeneration´ Build Configurations, and requires that
// * the `Kingdom.OrTools.Sat.CodeGeneration.Attributes´ package be build and deployed
// * to internal NuGet Package Sources. */
//[assembly: SupportProtocolBufferParameters]
